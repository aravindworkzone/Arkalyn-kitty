Build the Arkalyn-Kitty landing page.

Sections (in order):
1. Top nav: logo (Arkalyn-Kitty), "How it works", "Features", "FAQ", language toggle (EN/தமிழ்), theme toggle, Login button (ghost), Register button (primary)
2. Hero: H1 "One shared kitty for the whole group", subtext explaining pool model, primary CTA "Start a kitty", secondary "See how it works", product screenshot of group dashboard with visible pool balance
3. Use cases strip: 4 cards with icons — Roommates (rent split), Trips (vacation kitty), Office (snack fund), Family (household pool)
4. How it works: 3 steps with illustrations — Create a group → Members contribute → Track expenses against pool. Make clear this is a POOL, not debt tracking.
5. Features grid: 6 cards — pooled wallet, multi-role permissions, category-wise reports, member exit flow, English + Tamil, light/dark themes
6. Security/trust: row of trust signals (encrypted, role-based access, audit log)
7. FAQ: 6 expandable questions
8. Final CTA: large card with "Start your first kitty" button
9. Footer: links, contact, copyright

Required: light + dark themes, mobile (375px) + desktop (1280px). No pricing tiers. No fake testimonials.